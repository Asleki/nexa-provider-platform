import test from "node:test";import assert from "node:assert/strict";import {createNationalMapClient} from "../../../src/map/nngla/national-map-client.js";
const body={authorityId:"authority:nngla",countryId:"country:novegeo",readRuntime:"simulation",items:[]};
test("Bundle 22B client performs HTTP GET with bbox and never needs database credentials",async()=>{let seen;const fetchRef=async(url,opts)=>{seen={url,opts};return {ok:true,status:200,headers:{get:()=>null},json:async()=>body};};const client=createNationalMapClient({apiBaseUrl:"http://127.0.0.1:8000",fetchRef});await client.readViewport({minLongitude:30,minLatitude:-20,maxLongitude:32,maxLatitude:-17},{families:["ROAD"]});assert.match(seen.url,/nngla-map\/features/);assert.match(seen.url,/minLongitude=30/);assert.doesNotMatch(JSON.stringify(seen),/PGHOST|PGPASSWORD|postgresql:\/\//i);});
