import test from "node:test";
import assert from "node:assert/strict";
import { mountHydrologyAtmospherePresentation } from "../../../src/map/environment/hydrology-atmosphere-presentation.js";
function ctx(){return{setTransform(){},clearRect(){},save(){},restore(){},beginPath(){},moveTo(){},lineTo(){},closePath(){},clip(){},fillRect(){},ellipse(){throw new Error("ellipse boundary must not render")},arc(){},fill(){},stroke(){},setLineDash(){},set fillStyle(v){},set strokeStyle(v){},set lineWidth(v){},set lineCap(v){},set lineJoin(v){},set globalCompositeOperation(v){}}}
function canvas(){const a={};return{style:{},setAttribute(k,v){a[k]=v},getAttribute(k){return a[k]},getContext(){return ctx()}}}
test("P005.3/P005.4 environment presentation is additive, visible and idempotent",()=>{const boundary=canvas();boundary.setAttribute("data-role","novegeo-map-canvas");const physical=canvas();physical.setAttribute("data-role","novegeo-physical-land-canvas");const coord=canvas();coord.setAttribute("data-role","novegeo-full-viewport-coordinate-canvas");const children=[boundary,physical,coord];const container={style:{},clientWidth:420,getBoundingClientRect(){return{width:420}},querySelector(sel){const role=sel.match(/'([^']+)'/)?.[1];return children.find(x=>x.getAttribute?.("data-role")===role)||null},insertBefore(n,b){children.splice(children.indexOf(b),0,n)},appendChild(n){children.push(n)}};const doc={querySelector(){return container},createElement(){return canvas()}};const r=mountHydrologyAtmospherePresentation(doc,{devicePixelRatio:1});assert.equal(r.status,"RENDERED");assert.equal(r.riverCount,5);assert.equal(r.lakeCount,3);assert.equal(r.rainfallSystemCount,2);assert.equal(r.surroundingWaterContext,true);assert.equal(r.rainfallPresentation,"irregular_intensity_field");assert.equal(children.filter(x=>x.getAttribute?.("data-role")==="novegeo-hydrology-atmosphere-canvas").length,1);const r2=mountHydrologyAtmospherePresentation(doc,{devicePixelRatio:1});assert.equal(r2.status,"RENDERED");assert.equal(children.filter(x=>x.getAttribute?.("data-role")==="novegeo-hydrology-atmosphere-canvas").length,1);});
